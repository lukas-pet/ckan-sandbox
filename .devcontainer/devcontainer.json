{
    "name": "CKAN Sandbox",
    
    "dockerComposeFile": [
        "../docker-compose.yml"
    ],
    "service": "ckan",
    "workspaceFolder": "/workspaces/ckan-sandbox"
    // "build": {
    //     "dockerfile": "../Dockerfile"
    // },`
    // "runArgs": [
    //     "--network=ckan-network"
    // ],
    // Optional: set workspace folder if needed
    // "workspaceFolder": "/workspaces/ckan-sandbox"
}